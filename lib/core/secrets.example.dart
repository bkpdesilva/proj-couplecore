/// Copy this file to lib/core/secrets.dart (gitignored — never commit the
/// real key) and fill in a real Gemini API key from
/// https://aistudio.google.com/apikey before running the app;
/// AiCounselorService needs it to reach Gemini.
const String kGeminiApiKey = 'REPLACE_WITH_YOUR_GEMINI_API_KEY';
